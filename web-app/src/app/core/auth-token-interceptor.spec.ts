import { AuthTokenInterceptor } from './auth-token-interceptor';

describe('AuthTokenInterceptor', () => {
  it('should create an instance', () => {
    expect(new AuthTokenInterceptor()).toBeTruthy();
  });
});
